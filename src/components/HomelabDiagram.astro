<div class="homelab-diagram" aria-label="Homelab topology diagram">
  <p class="terminal-brand homelab-label">k3s-cluster.internal</p>
  <div class="homelab-row">
    <div class="node">Gateway\nTraefik</div>
    <div class="node">Control Plane\nK3s</div>
    <div class="node">Worker\nApps + APIs</div>
  </div>
  <p class="homelab-note">
    GitOps-managed services behind Traefik with metrics and tracing via Prometheus,
    Grafana, and Jaeger.
  </p>
</div>

<style>
  .homelab-diagram {
    border: 1px solid var(--color-border);
    border-radius: 0.9rem;
    background: linear-gradient(135deg, var(--color-bg-secondary), var(--color-bg-primary));
    padding: 1rem;
    display: grid;
    gap: 0.8rem;
  }

  .homelab-label {
    margin: 0;
    color: var(--color-accent);
    font-size: 0.9rem;
  }

  .homelab-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 0.6rem;
  }

  .node {
    white-space: pre-line;
    text-align: center;
    border: 1px solid var(--color-border);
    border-radius: 0.7rem;
    background: var(--color-bg-tertiary);
    color: var(--color-text-primary);
    padding: 0.8rem;
    font-size: 0.9rem;
  }

  .homelab-note {
    margin: 0;
    font-size: 0.95rem;
  }
</style>
