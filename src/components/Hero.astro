<section id="hero" class="section hero-section">
  <div class="container hero-inner">
    <p class="terminal-brand hero-tagline">&gt; software_engineer --focus="infra,devops,ai"</p>
    <h1>Ej Sadiarin</h1>
    <p class="hero-summary">
      Software engineer building resilient infrastructure, developer platforms, and
      AI-enabled backend systems.
    </p>
    <div class="hero-actions" aria-label="Hero calls to action">
      <a class="cta-primary" href="#projects">View Projects</a>
      <a class="cta-secondary" href="#contact">Contact Me</a>
    </div>
    <div class="hero-social" aria-label="Social links">
      <a href="https://github.com/ejsadiarin" target="_blank" rel="noreferrer">GitHub</a>
      <a href="https://www.linkedin.com/in/edwinsadiarin" target="_blank" rel="noreferrer">LinkedIn</a>
      <a href="mailto:ejsadiarin@gmail.com">Email</a>
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding-top: 5.5rem;
  }

  .hero-inner {
    display: grid;
    gap: 1.2rem;
  }

  .hero-tagline {
    margin: 0;
    color: var(--color-accent);
    font-size: 0.95rem;
  }

  h1 {
    margin: 0;
    color: var(--color-text-primary);
    font-size: clamp(2rem, 5vw, 3.4rem);
    line-height: 1.1;
  }

  .hero-summary {
    margin: 0;
    max-width: 42rem;
    font-size: 1.05rem;
  }

  .hero-actions,
  .hero-social {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
  }

  .hero-actions a,
  .hero-social a {
    border: 1px solid var(--color-border);
    border-radius: 0.55rem;
    padding: 0.55rem 0.9rem;
    transition: border-color 0.2s ease, color 0.2s ease, background-color 0.2s ease;
  }

  .cta-primary {
    color: var(--color-bg-primary);
    border-color: var(--color-accent);
    background: var(--color-accent);
    font-weight: 600;
  }

  .cta-primary:hover,
  .cta-primary:focus-visible {
    background: var(--color-accent-hover);
    border-color: var(--color-accent-hover);
  }

  .cta-secondary {
    color: var(--color-text-primary);
  }

  .cta-secondary:hover,
  .cta-secondary:focus-visible,
  .hero-social a:hover,
  .hero-social a:focus-visible {
    color: var(--color-accent);
    border-color: var(--color-accent);
  }
</style>
