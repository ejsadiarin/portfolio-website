<header data-fixed-nav="true" class="site-header">
  <div class="container header-inner">
    <a href="#hero" data-terminal-brand="true" class="terminal-brand brand">ej@portfolio:~$</a>
    <nav aria-label="Primary" class="header-nav">
      <a href="#hero">Hero</a>
      <a href="#about">About</a>
      <a href="#projects">Projects</a>
      <a href="#experience">Experience</a>
      <a href="#skills">Skills</a>
      <a href="#homelab">Homelab</a>
      <a href="#blog">Blog</a>
      <a href="#contact">Contact</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    width: 100%;
    border-bottom: 1px solid var(--color-border);
    background: color-mix(in srgb, var(--color-bg-primary) 88%, transparent);
    backdrop-filter: blur(8px);
  }

  .header-inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem;
    min-height: 4.5rem;
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
  }

  .brand {
    color: var(--color-accent);
    font-weight: 600;
  }

  .header-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    font-size: 0.92rem;
  }

  .header-nav a {
    color: var(--color-text-secondary);
  }

  .header-nav a:hover,
  .header-nav a:focus-visible {
    color: var(--color-accent);
  }
</style>
